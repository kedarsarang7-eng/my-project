org.gradle.jvmargs=-Xmx6G -XX:MaxMetaspaceSize=1G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false
org.gradle.caching=true
org.gradle.parallel=true
# Reuse the Gradle daemon and watch the filesystem for faster incremental builds.
org.gradle.daemon=true
org.gradle.vfs.watch=true
# Fix cross-drive Kotlin incremental compilation cache error
# (pub cache on C:\ vs project on G:\ causes IllegalArgumentException)
kotlin.incremental=false

