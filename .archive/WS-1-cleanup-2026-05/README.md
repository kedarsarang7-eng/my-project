# WS-1 Cleanup Quarantine — 2026-05

This directory contains files moved from `Dukan_x/` during workstream **WS-1** of the senior architect audit. Nothing here is referenced by the application code. Files are staged here so the team can verify before final deletion.

## Contents

### `aws-credentials/` — DELETE AFTER VERIFYING SECRETS MANAGER PARITY
Local copies of AWS credentials. Already gitignored (never committed) but should not exist on developer machines.

| File | Purpose | Replacement |
|------|---------|-------------|
| `aws_secrets.json` | Secret values dump | AWS Secrets Manager |
| `aws_kms.json` | KMS key references | AWS Console / IaC |
| `aws_secret_val.json` | Secret values dump (older) | AWS Secrets Manager |
| `aws_eips.json` | Elastic IP records | AWS Console / IaC |

**Action required:** Verify all values exist in AWS Secrets Manager / SSM Parameter Store, then delete this folder.

### `Dukan_x-logs/` — SAFE TO DELETE
Build/analysis output that was committed to disk by mistake. Reproducible from `flutter analyze`, `flutter pub outdated`, `dart run build_runner build`.

### `Dukan_x-fix-scripts/` — REVIEW BEFORE DELETING
One-shot migration/cleanup scripts. Most are obsolete (e.g., `fix_unused_imports.ps1` after we adopted `flutter_lints`). Keep any that document a still-useful procedure under `scripts/` at repo root with a doc-comment explaining when to run them.

### `Dukan_x-env-duplicates/` — DELETE AFTER VERIFYING NO TOOLING REFERENCES THEM
Three duplicate env files that overlapped with the canonical `.env`, `.env.staging`, `.env.production` (kept in `Dukan_x/`).

| File | Reason quarantined |
|------|--------------------|
| `.env.dev` | Duplicate of `.env` (same Cognito IDs, same API URL) |
| `.env.development` | Placeholder version, redundant with `.env.example` |
| `.env.prod` | Subset of `.env.production` |

### `Dukan_x-misc/` — REVIEW BEFORE DELETING
| File | Reason |
|------|--------|
| `DukanX.iml`, `flutter_application_1.iml` | Legacy IntelliJ project descriptors. The `.iml` is regenerated by IDE on open. The second name is leftover from initial `flutter create` scaffold. |
| `ecosystem.config.js` | PM2 config — only relevant if hosting Node backend on EC2. Backend is now Lambda. |
| `_audit.txt`, `_audit_errors.txt` | Output from prior audit runs. |

## How to finalize

After 1–2 weeks of running the app to confirm nothing was missed:

```powershell
Remove-Item -Recurse -Force "g:\desktop app genuine\.archive\WS-1-cleanup-2026-05"
```
