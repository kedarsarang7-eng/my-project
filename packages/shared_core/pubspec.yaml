name: shared_core
description: Shared core package for DukanX apps - DTOs, API clients, validators
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  dio: ^5.4.0
  flutter_riverpod: ^2.4.9
  flutter_secure_storage: ^9.0.0
  freezed_annotation: ^2.4.1
  json_annotation: ^4.8.1
  jwt_decoder: ^2.0.1
  logger: ^2.0.2
  retry: ^3.1.2

  # For Flutter
  flutter:
    sdk: flutter

dev_dependencies:
  build_runner: ^2.4.7
  flutter_test:
    sdk: flutter
  freezed: ^2.4.5
  json_serializable: ^6.7.1
  mockito: ^5.4.4
  very_good_analysis: ^5.1.0

flutter:
