{
    "name": "dukanx-websocket-service",
    "version": "1.0.0",
    "description": "WebSocket real-time communication service for DukanX",
    "scripts": {
        "build": "tsc",
        "deploy": "npm run build && npx serverless deploy --stage dev"
    },
    "dependencies": {
        "@aws-sdk/client-dynamodb": "^3.700.0",
        "@aws-sdk/client-apigatewaymanagementapi": "^3.700.0",
        "aws-jwt-verify": "^4.0.0",
        "zod": "^3.22.0"
    },
    "devDependencies": {
        "typescript": "^5.3.0",
        "@types/aws-lambda": "^8.10.130",
        "@types/node": "^20.0.0",
        "serverless": "^4.0.0"
    }
}