#!/usr/bin/env node
/**
 * Non-interactive `audit` CLI entry point.
 *
 * Wires the Config Loader through reporting with no interactive input,
 * invokes the orchestrator, and propagates the exit code (Requirement 14.4).
 */
export {};
