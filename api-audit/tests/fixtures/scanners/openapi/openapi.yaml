# Fixture: OpenAPI document.
# Exercises the OpenAPI scanner's path + method + operationId extraction
# (Requirement 1.3).
openapi: 3.0.0
info:
  title: DukanX Test API
  version: 1.0.0
paths:
  /health:
    get:
      operationId: getHealth
      responses:
        '200':
          description: OK
  /customers/{id}:
    get:
      operationId: getCustomer
      responses:
        '200':
          description: OK
    delete:
      operationId: deleteCustomer
      responses:
        '204':
          description: No Content
